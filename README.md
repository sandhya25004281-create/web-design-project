# web-design-project
A clean and responsive pricing table built using only HTML and CSS, designed for websites, landing pages. It features multiple pricing plans with feature lists, clear call-to-action buttons, and a highlighted recommended plan. The layout is fully responsive across devices and easy to customize through CSS variables for colors, spacing, and fonts. 
A simple, modern, and responsive pricing table component created with semantic HTML and CSS. It includes multiple pricing tiers, feature lists, CTA buttons, and a highlight for the recommended plan. No JavaScript required — easy to drop into any website or project.

Features

Responsive layout (works across mobile, tablet, and desktop)

Three pricing tiers (Basic, Pro, Enterprise) with a highlighted recommended plan

Semantic HTML for better SEO and accessibility

CSS-only solution (flexbox/grid + CSS variables)

Easy to customize colors, fonts, and spacing via CSS variables

Clean, minimal design ready to be used in landing pages or demos
